{
  "$schema": "http://json-schema.org/schema",
  "id": "ng-toolkit firebug",
  "title": "Angular Application Options Schema",
  "type": "object",
  "properties": {
    "clientProject": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "skipInstall": {
      "description": "Skip installation of dependencies.",
      "type": "boolean",
      "default": false
    }
  },
  "required": []
}